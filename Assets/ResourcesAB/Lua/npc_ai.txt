local define = require('commmons/define')
local ActorState = define.ActorState

local idle = {}
function idle.Enter(self)

end

function idle.Check(self)
    return false
end

function idle.Exit(self)

end

local run = {}
function run.Enter(self)

end

function run.Check(self)
    return false
end

function run.Exit(self)

end

local _M = {
    [ActorState.IDLE] = idle,
    [ActorState.RUN] = run,
}

return _M
